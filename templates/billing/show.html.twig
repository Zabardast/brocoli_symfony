{% extends 'base.html.twig' %}

{% block title %}Billing{% endblock %}

{% block body %}
    <h1>Billing</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ billing.id }}</td>
            </tr>
            <tr>
                <th>Billing_number</th>
                <td>{{ billing.billingNumber }}</td>
            </tr>
            <tr>
                <th>Entitled</th>
                <td>{{ billing.entitled }}</td>
            </tr>
            <tr>
                <th>Biling_status</th>
                <td>{{ billing.bilingStatus }}</td>
            </tr>
            <tr>
                <th>Creation_date</th>
                <td>{{ billing.creationDate }}</td>
            </tr>
            <tr>
                <th>Payment_deadline</th>
                <td>{{ billing.paymentDeadline }}</td>
            </tr>
            <tr>
                <th>Payment_method</th>
                <td>{{ billing.paymentMethod }}</td>
            </tr>
            <tr>
                <th>Time_of_payment</th>
                <td>{{ billing.timeOfPayment }}</td>
            </tr>
            <tr>
                <th>Details</th>
                <td>{{ billing.details }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('billing_index') }}">back to list</a>

    <a href="{{ path('billing_edit', {'id': billing.id}) }}">edit</a>

    {{ include('billing/_delete_form.html.twig') }}
{% endblock %}
