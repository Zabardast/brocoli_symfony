{% extends 'base.html.twig' %}

{% block title %}Line{% endblock %}

{% block body %}
    <h1>Line</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ line.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ line.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ line.price }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ line.quantity }}</td>
            </tr>
            <tr>
                <th>About</th>
                <td>{{ line.about }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('line_index') }}">back to list</a>

    <a href="{{ path('line_edit', {'id': line.id}) }}">edit</a>

    {{ include('line/_delete_form.html.twig') }}
{% endblock %}
